# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $run_rofi "QT_AUTO_SCREEN_SCALE_FACTOR=0 QT_SCALE_FACTOR=0 ensure-us-layout 'rofi -combi-modi drun,window -modi combi -show combi -lines 6 -show-icons'"
set $run_rofi_emoji "ensure-us-layout rofi-emoji-launch"

set $mod Mod4

set $Slash_Question 61
set $Slash_Pipe 51
# qwertyuiop
set $code_q 24
set $code_w 25
set $code_e 26
set $code_r 27
set $code_t 28
set $code_y 29
set $code_u 30
set $code_i 31
set $code_o 32
set $code_p 33
# asdfghjkl
set $code_a 38
set $code_s 39
set $code_d 40
set $code_f 41
set $code_g 42
set $code_h 43
set $code_j 44
set $code_k 45
set $code_l 46
# zxcvbnm
set $code_z 52
set $code_x 53
set $code_c 54
set $code_v 55
set $code_b 56
set $code_n 57
set $code_m 58

####################
# Script assigners #
####################

set $stoprec --no-startup-id killall -q ffmpeg
set $screencast  --no-startup-id bash screencast_pulse
set $term --no-startup-id terminator

###################
# Spawn dropdowns #
###################
# exec --no-startup-id $term --title ddtmux -e tmux

# Make the currently focused window a scratchpad
bindsym $mod+Shift+period move scratchpad
# Show the first scratchpad window
bindsym $mod+period scratchpad show move position center

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:Sans 9, Lato 9

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

bindsym $mod+Return exec --no-startup-id $term
bindcode $mod+$code_i exec --no-startup-id $term -e htop
bindcode $mod+$code_a exec --no-startup-id $term -e ranger
bindcode $mod+$code_n exec --no-startup-id nautilus
bindcode $mod+$code_w exec --no-startup-id google-chrome-stable

bindcode $mod+shift+$code_t exec --no-startup-id typora
bindcode $mod+shift+$code_a exec --no-startup-id atom
bindcode $mod+shift+$code_s exec --no-startup-id code

bindcode $mod+$code_q kill

bindcode $mod+Shift+$code_d exec --no-startup-id $run_rofi_emoji
bindcode $mod+$code_d exec --no-startup-id $run_rofi

# change focus
bindcode $mod+$code_h focus left
bindcode $mod+$code_j focus down
bindcode $mod+$code_k focus up
bindcode $mod+$code_l focus right

###################################
# Resize without the resize mode #
###################################
bindcode $mod+Shift+$code_y exec --no-startup-id i3resize left
bindcode $mod+Shift+$code_u exec --no-startup-id i3resize down
bindcode $mod+Shift+$code_i exec --no-startup-id i3resize up
bindcode $mod+Shift+$code_o exec --no-startup-id i3resize right

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindcode $mod+Shift+$code_h move left
bindcode $mod+Shift+$code_j move down
bindcode $mod+Shift+$code_k move up
bindcode $mod+Shift+$code_l move right

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right


#########
# Modes #
#########
# split in horizontal orientation
bindcode $mod+Shift+$code_v split h
# split in vertical orientation
bindcode $mod+$code_v split v
#
# enter fullscreen mode for the focused container
bindcode $mod+$code_f fullscreen toggle
# position window to center of the screen
bindcode $mod+$code_g move position center
# change container layout (stacked, tabbed, toggle split)
# bindcode $mod+$code_s layout stacking
# bindcode $mod+$code_w layout tabbed
bindcode $mod+$code_e layout toggle split
# toggle tiling / floating
bindcode $mod+$code_t floating toggle
# change focus between tiling / floating windows
bindsym $mod+Tab focus mode_toggle

# focus the parent container
bindcode $mod+$code_p focus parent
# focus the child container
bindcode $mod+$code_c focus child

##############
# Workspaces #
##############
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# Toggle float mode for specific apps
for_window [class="Gnome-calculator"] floating enable
for_window [class="Gnome-calculator"] move position center

for_window [class="Typora"] floating enable resize set 670 750
for_window [class="Typora"] move position center

for_window [class="Peek"] floating enable resize set 600 300
for_window [class="Peek"] move position center

for_window [class="slack"] floating enable resize set 970 880
for_window [class="slack"] move position center

for_window [class="wavebox"] floating enable resize set 1470 980 move position center
for_window [class="wavebox"] move position center

for_window [title="Messenger for Desktop"] floating enable resize set 900 550, move absolute position 20 500

for_window [class="Enpass"] floating enable resize set 600 300
for_window [class="Enpass"] move position center

for_window [window_role="pop-up"] floating enable
for_window [window_role="bubble"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [window_role="Preferences"] floating enable

for_window [window_type="dialog"] floating enable
for_window [window_type="menu"] floating enable
#####################################
# Assign applications to workspaces #
#####################################
assign [class="Atom"] $ws3
assign [class="Code"] $ws3
# assign [class="Enpass"] $ws7
assign [class="Firefox"] $ws2
assign [class="Brave-browser"] $ws2
assign [class="Google-chrome"] $ws2
assign [class="Slack"] $ws4
assign [class="wavebox"] $ws4
assign [title="Messenger for Desktop"] $ws5
for_window [title="^Enpass$"] move container to workspace $ws7
for_window [class="Spotify"] move to workspace $ws10

# Reload the configuration file
bindcode $mod+Shift+$code_c reload
# Restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindcode $mod+Shift+$code_r restart
# Exit i3 (logs you out of your X session)
# bindcode $mod+Shift+$code_e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -b 'Yes, exit i3' 'i3-msg exit'"

bindcode $mod+$code_r mode "resize"

mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindcode $code_h resize shrink width 10 px or 10 ppt
        bindcode $code_j resize grow height 10 px or 10 ppt
        bindcode $code_k resize shrink height 10 px or 10 ppt
        bindcode $code_l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape
        bindsym Return mode "default"
        bindsym Escape mode "default"
}

#########################################
#=========== Custom Commands ===========#
#########################################
# Set keyboard through xorg
bindcode $mod+Shift+$Slash_Pipe exec --no-startup-id set-xkeyboard
# Set background
bindsym $mod+Shift+Print exec --no-startup-id unsplash-generator
# Set a random wallpaper from ~/Pictures/Wallpapers
bindsym $mod+Print exec --no-startup-id random-wallpaper
# Change to greek keyboard layout
bindsym $mod+space exec --no-startup-id xkb-switch -n
bindsym $mod+Escape exec --no-startup-id xkb-switch -n

# Screen layout generated from arandr
exec --no-startup-id source horizontal-laptop-right
# Assign profile switch for bose35 headset to keyboard shortcut
bindcode Control+Shift+$Slash_Question exec --no-startup-id bose35-profile-switch
bindcode $mod+shift+$code_x exec --no-startup-id blur-lock

# Keyboard media controls
bindsym $mod+F2 exec --no-startup-id amixer set Capture 2%-
bindsym $mod+F3 exec --no-startup-id amixer set Capture 2%+
bindsym XF86AudioLowerVolume exec --no-startup-id amixer -q -D pulse sset Master 2%-
bindsym XF86AudioRaiseVolume exec --no-startup-id amixer -q -D pulse sset Master 2%+
bindsym XF86AudioMute exec --no-startup-id amixer -q -D pulse sset Master toggle
bindsym XF86AudioMicMute exec --no-startup-id amixer set Capture toggle

bindsym $mod+shift+F3 exec --no-startup-id $HOME/dotfiles/scripts/polybar/pulse-input --volume_source_up
bindsym $mod+shift+F2 exec --no-startup-id $HOME/dotfiles/scripts/polybar/pulse-input --volume_source_down

bindsym $mod+F4 exec --no-startup-id amixer set Capture toggle

# Sreen brightness controls
# Requires light package from aur
bindsym XF86MonBrightnessUp exec --no-startup-id light -A 10 # increase screen brightness
bindsym XF86MonBrightnessDown exec --no-startup-id light -U 10 # decrease screen brightness

# Media player controls
bindsym $mod+F10 exec --no-startup-id playerctl previous
bindsym $mod+F11 exec --no-startup-id playerctl play-pause
bindsym $mod+F12 exec --no-startup-id playerctl next

bindsym XF86AudioPrev exec --no-startup-id playerctl previous
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
# bindsym XF86WLAN exec --no-startup-id wifi-toggle
bindsym $mod+Shift+Delete	exec --no-startup-id playerctl pause; exec --no-startup-id amixer set Capture nocap; exec --no-startup-id pkill mpv; workspace 9; exec --no-startup-id $term -e htop; exec --no-startup-id $term -e ranger


#################################
# Always start (on reload etc...) #
#################################
# Specific directive to enable tap to click on touchpad
exec_always --no-startup-id enable-touchpad-tapping
exec_always --no-startup-id $HOME/dotfiles/polybar/.config/polybar/launch.sh
exec_always --no-startup-id xrdb ~/.Xresources
# exec_always --no-startup-id set-xkeyboard
exec_always --no-startup-id feh --bg-fill ~/.config/wall.png
exec_always --no-startup-id compton --config ~/.config/compton.conf -b -f

########################
# Applications Startup #
########################
exec --no-startup-id idle-hook
exec --no-startup-id dunst
exec --no-startup-id nm-applet --indicator
exec --no-startup-id spotify
exec --no-startup-id dropbox
exec --no-startup-id wavebox
exec --no-startup-id redshift-gtk -l 35.1:25.7
exec --no-startup-id parcellite
exec --no-startup-id enpass


##############################################
#=========== Image/Video Commands ===========#
##############################################
######################
# Screenshot helpers #
######################
bindsym Control+Shift+F1 exec --no-startup-id take-screenshot imgur
bindsym Shift+Print --release exec --no-startup-id take-screenshot window
bindsym Print --release exec --no-startup-id take-screenshot screen
bindsym Control+Shift+F4 exec --no-startup-id take-screenshot region

##############
# Screencast #
##############
set $screencastmode " :  (0)Laptop -  (1)Main"
mode  $screencastmode {
    bindsym 0 exec --no-startup-id screencast 0; mode "default"
    bindsym 1 exec --no-startup-id screencast 1; mode "default"
    bindcode $code_q mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Insert mode $screencastmode
bindsym $mod+Delete exec --no-startup-id $stoprec; exec --no-startup-id notify-send " Stopped recording"


##############################################
#================== Modes ===================#
##############################################

####################
# Bluetooth Headet #
####################
set $bluetooth_headset "  (0)Disconnect -   (1)Connect"
mode  $bluetooth_headset {
    bindsym 0 exec --no-startup-id disconnect-from-bose; mode "default"
    bindsym 1 exec --no-startup-id connect-to-bose; mode "default"
    bindcode $code_q mode "default"
    bindsym Escape mode "default"
}
bindsym $mod+Shift+F10 mode $bluetooth_headset
###################
# Display chooser #
###################
set $displays "    (0)Laptop -     (1)Main -     (2)H -     (3)V"
mode  $displays {
    bindsym 0 exec --no-startup-id laptop-screen; mode "default"
    bindsym 1 exec --no-startup-id one-screen; mode "default"
    bindsym 2 exec --no-startup-id horizontal-laptop-right; mode "default"
    bindsym 3 exec --no-startup-id vertical-layout-two-screens; mode "default"
    bindcode $code_q mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Shift+F7 mode $displays
###################
# Session manager #
###################
set $session " (E)xit - (L)ock -  (P)oweroff -  (R)eboot -  (S)uspend"
mode  $session {
    bindcode $code_e exec --no-startup-id $psst i3-msg exit; mode "default"
    # bindcode $code_l exec --no-startup-id "setxkbmap -layout us,gr"; mode "default"
    bindcode $code_l exec --no-startup-id blur-lock; mode "default"
    # bindcode $code_l exec --no-startup-id "setxkbmap -layout us"; exec --no-startup-id dm-tool lock; mode "default"
    bindcode $code_p exec --no-startup-id $psst systemctl poweroff; mode "default"
    bindcode $code_r exec --no-startup-id $psst systemctl reboot; mode "default"
    bindcode $code_s exec --no-startup-id $psst systemctl suspend; mode "default"

    bindcode $code_q mode "default"
    bindsym Escape mode "default"
}

bindsym $mod+Shift+F4 mode $session
#########
# Theme #
#########
# set primary gruvbox colorscheme colors
set $aqua #689d68
set $bg #282828
set $black #000000
set $blue #458588
set $darkgray #1d2021
set $gray #a89984
set $green #98971a
set $purple #b16286
set $red #cc241d
set $softYellow #fbf1c7
set $transparent_bg #88282828
set $white #ffffff
set $yellow #d79921

# Bindings to make the webcam float and stick.
for_window [class="MPlayer"] floating enable
for_window [class="MPlayer"] sticky enable
for_window [class="MPlayer"] border pixel 0
no_focus [class="MPlayer"]

bindsym $mod+Home exec --no-startup-id camtoggle

# class                 border|backgr|text|indicator|child_border
client.focused          $darkgray $blue $darkgray $purple $blue
client.focused_inactive $darkgray $darkgray $blue $purple $darkgray
client.unfocused        $darkgray $darkgray $blue $purple $darkgray
client.urgent           $red $red $blue $red $red

######################
# i3 Bar with Blocks #
######################

# bar {
# 		status_command i3blocks -c ~/.config/i3blocks/i3blocks.conf
# 		tray_output             primary
# 		separator_symbol        "|"
# 		workspace_buttons       yes
# 		position                top
# 		colors {
# 				background  #212121
# 				statusline  #DDDDDD
# 				separator   #777777
# 				# class             border  backgr. text
# 				focused_workspace   $darkgray $blue $darkgray $purple $white
# 				active_workspace    $darkgray $darkgray $white $purple $darkgray
# 				inactive_workspace  $darkgray $darkgray $white $purple $darkgray
# 				urgent_workspace    $red $red $white $red $red
# 		}
# }
#end of window title bars & borders section

# Needed for i3-gaps
hide_edge_borders smart
for_window [class="^.*"] border pixel 2

gaps inner 8
gaps outer 2

bindsym $mod+Shift+minus gaps inner all plus 4
bindsym $mod+Shift+plus gaps inner all minus 4
bindsym $mod+Mod1+Shift+minus gaps inner current plus 4
bindsym $mod+Mod1+Shift+plus gaps inner current minus 4

# Toggle border for all open windows
bindcode $mod+$code_b [class="^.*"] border toggle
new_window normal 0
new_float normal 0

bindcode $mod+Mod1+$code_h move workspace to output left
bindcode $mod+Mod1+$code_l move workspace to output right
bindcode $mod+Mod1+$code_j move workspace to output down
bindcode $mod+Mod1+$code_k move workspace to output up
bindcode $mod+Control+$code_l workspace next
bindcode $mod+Control+$code_h workspace prev
bindsym Control+Tab workspace next
bindsym Control+Shift+Tab workspace prev


# Activate 1px border on active window
# exec_always --no-startup-id bash ~/.config/scripts/inactive-handler

# i3-gnome
# exec --no-startup-id /usr/lib/gnome-settings-daemon/gsd-xsettings
# exec_always --no-startup-id gnome-power-manager
# exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
# exec --no-startup-id gnome-flashback
# exec_always --no-startup-id gsettings set org.gnome.desktop.interface cursor-size 8
